# bool

the bool datatype `false`, `true`

# implementation

it is usually stored as a byte, instead of a single bit, because a binary computer is streamlined to work with bytes instead of individual bits.

[<code>false</code>]: constants/bool#false
[<code>true</code>]: constants/bool#true
