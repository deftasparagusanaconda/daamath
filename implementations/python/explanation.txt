we have certain .py files that can be generated from the .yaml files in the specification.

run `python generate.py` to create src/, which copies handwritten/, and generates whatever other .py files are needed from specification

as of now the following are generated:
- src/daamath/strings/*.py
