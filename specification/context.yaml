context:
  functions:
    _some function_:
      definition: function pointer
      inputs:
        _some input_:
          domain: domain
          check_domain: bool
          OutOfDomainError_policy: enum
        …: …
      output:
        domain: domain
        check_domain: bool
        OutOfDomainError_policy: enum
    …: …
  datatypes:
    _some datatype_:
      sentinel_value: any
      FidelityError_policy: enum
      sentinel_value_policy: enum
      rounding: enum
    …: …
