daamath is hosted on PyPI. simply run this in your terminal:

```
python -m pip install daamath
```

you may have to set up a [venv](https://docs.python.org/3/library/venv.html#creating-virtual-environments). once youre done, try this:

```
import daamath as dm

print(dm.sqrt(4))
# 2
```

